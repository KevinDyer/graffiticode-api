rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /code-hashes/{codeHash} {
      allow read, write: if true;
    }

    match /tasks/{task} {
      allow read, write: if true;
    }
  }
}